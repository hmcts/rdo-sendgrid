parameters:
  env: ''

variables:
  b: $[ <expression> ]

steps:
- script: |
    echo 'nonprod'
    echo $(nonprod-config)
  condition: eq('${{ parameters.env }}', 'nonprod')
- script: |
    echo 'prod'
    echo $(prod-config)
  condition: eq('${{ parameters.env }}', 'prod')
  
